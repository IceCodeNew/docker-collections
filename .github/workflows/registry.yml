name: registry Image CI
permissions: {}

on:
  push:
    branches:
      - "master"
    paths:
      - ".github/workflows/registry.yml"
      - "registry/base.yaml"
      - "registry/Dockerfile"
  schedule:
    - cron: "10 3 1-31/10 * *"
  workflow_dispatch:

env:
  REGISTRY_IMAGE: ${{ secrets.DOCKERHUB_USERNAME }}/registry
  # renovate: datasource=docker packageName=library/registry versioning=docker
  REGISTRY_VERSION: 3.1.1

jobs:
  build-registry-base:
    name: Build registry-base
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Install apko
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          cache: true
          mise_toml: |
            [tools]
            apko = "latest"

      - name: Build registry-base
        shell: bash
        env:
          REGISTRY_IMAGE: ${{ env.REGISTRY_IMAGE }}
        run: |
          apko publish ./registry/base.yaml "${REGISTRY_IMAGE}:base"

  registry:
    runs-on: ubuntu-24.04
    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@99012661954931238ded8c8b007157a8430204e1 # v4.4.0
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@f87e5991a6d7451dcb8d9637bfbc97413f497069 # v4.4.1
      -
        name: Login to Docker Hub
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and load the current arch image to local daemon
        uses: docker/build-push-action@c3c9e263c25d99ce0380d002d59b67737d91b0dc # v7.4.0
        with:
          context: "{{defaultContext}}:registry"
          cache-from: type=gha
          cache-to: type=gha,mode=max
          load: true
          tags: ${{ env.REGISTRY_IMAGE }}:${{ env.REGISTRY_VERSION }}
      - name: Smoke Test
        env:
          REGISTRY_IMAGE: ${{ env.REGISTRY_IMAGE }}
          REGISTRY_VERSION: ${{ env.REGISTRY_VERSION }}
        run: |
          docker save -o "registry.tar" "${REGISTRY_IMAGE}:${REGISTRY_VERSION}"
      - name: Build and push
        uses: docker/build-push-action@c3c9e263c25d99ce0380d002d59b67737d91b0dc # v7.4.0
        with:
          context: "{{defaultContext}}:registry"
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ env.REGISTRY_IMAGE }}:${{ env.REGISTRY_VERSION }}
            ${{ env.REGISTRY_IMAGE }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Inspect image
        env:
          REGISTRY_IMAGE: ${{ env.REGISTRY_IMAGE }}
          REGISTRY_VERSION: ${{ env.REGISTRY_VERSION }}
        run: |
          docker buildx imagetools inspect "${REGISTRY_IMAGE}:${REGISTRY_VERSION}" \
              | grep -Po '[^ \r\n\t\v]+@sha256:[a-z0-9]+' | xargs -r -t -n 1 docker buildx imagetools inspect --raw
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          input: "registry.tar"
          format: "table"
